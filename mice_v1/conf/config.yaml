defaults:
  #- _self_
  - network: network  # 加载 conf/network/network.yaml
  # - train: train  # 加载 conf/train/train.yaml, 如果不是train可以不用加载
  - simulation: sim  # 加载 conf/simulation/sim.yaml，无论是sim还是train都要加载
  - dataset: noisydc_ou
  - input: e_only
  - generator: generator
  - _self_ #(如果有全局参数可以加这个，没有则不需要)

# 这里可以放一些跟 network 无关的全局参数
seed: 42
device: "cuda:0"
out_dir: "./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}"

bridge:
  use_generated_data: true

experiment_yaml: null