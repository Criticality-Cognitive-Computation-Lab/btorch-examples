type: noisydc_ou
# 模式选择: "fixed" (固定值) 或 "rheobase_scaled" (基于阈值)
current_mode: "rheobase_scaled" 

# 如果是 rheobase_scaled:
scale_factor: 1.0   # 1.05 倍阈值，意味着均值稍微超过阈值 (Mean-driven)
                      # 0.95 倍阈值，意味着亚阈值，靠噪声驱动 (Fluctuation-driven)


std: 0.7

tau_ou: 20.0

scale_noise_with_mean: True